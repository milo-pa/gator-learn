# Api Documentation

Both the frontend and backend need to be on the same page about what is being sent and what is being received, like prof said

Update this markdown file with a sample json output for each api page that gets added, including if it has an error format.