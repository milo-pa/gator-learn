name: CI/CD on Push to Main

on:
  push:
    branches: [ "feature/CI/CD" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Backend build (no tests)
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: "corretto"
          java-version: "21"

      - name: Build backend
        working-directory: ./application/backend
        run: |
          mvn -B clean package
          ls -l target

      # Frontend build (no tests)
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Build frontend
        working-directory: ./application/frontend
        run: |
          npm ci
          npm run build
          ls -R build

  deploy:
    needs: build
    runs-on: ubuntu-latest



    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Copy backend JAR to EC2
        working-directory: ./application/backend
        run: |
          JAR_PATH="target/SFSUTutoring-1.0.0.jar"
          if [ ! -f "$JAR_PATH" ]; then
            echo "Jar not found at $JAR_PATH"
            exit 1
          fi

          scp -o StrictHostKeyChecking=no "$JAR_PATH" \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:${{ secrets.EC2_BACKEND_RELEASE_DIR }}/SFSUTutoring-1.0.0.jar

      - name: Copy frontend build to EC2
        working-directory: ./application/frontend
        run: |
          if [ ! -d "build" ]; then
            echo "Frontend build directory not found"
            exit 1
          fi

          scp -o StrictHostKeyChecking=no -r build/* \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:${{ secrets.EC2_FRONTEND_DIR }}/

      - name: Run deploy script on EC2
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} \
            "${{ secrets.EC2_DEPLOY_SCRIPT }}"